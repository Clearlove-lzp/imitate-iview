import { getCustomUrl } from "../../base.js"; import { dns, encrypt } from "../../disposition.js"; var Config = function () { this.dns = "", this.profile = { is_wss: encrypt(), dns: dns }, this.power = { period_sec: 55, conn_timeout_sec: 90 }, this.audio = { tone_enable: !1, packet_frames: 5, codec_type: 101, amr_mode: 6, save_play: !1, save_record: !1 }, this.device = { platform: "Web", device: "PC", os: "Windows10", serialNumber: "A123456" } }; export default Config;